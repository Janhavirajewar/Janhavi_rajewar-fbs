// Write a C program to input the length and width of a rectangle and find its perimeter.

#include<stdio.h>
void main()
{

	float l=4,w=4;
	float peri=2*(l+w);
	printf("length and width of rectangle is %f,%f \n",l,w);
	printf("perimeter of rectangle is %f \n",peri);
	
}